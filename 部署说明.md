# 部署说明 —— 把本子装到 iPhone 上

## 为什么必须部署

双击 `index.html` 打开是能用，但那只是"看一眼"。要真正在手机上当 App 用，必须部署到 https 网址，原因有三个：

1. **「添加到主屏幕」只在 https 网址上才会变成全屏 App**，否则就是一个带地址栏的网页
2. **`file://` 协议下浏览器会禁用数据库**，你的待办存不下来
3. 只有 https 才能注册 Service Worker，做到**离线可用**（飞行模式也能打开）

---

## 🔴 一条铁律，先记住

> **部署完拿到的那个网址，永远不要再改。**

iPhone 上"添加到主屏幕"的 App，数据存在 **「这个网址」的独立沙盒**里。
**换网址 = 换 App = 一年日记和所有照片全部蒸发**，而且找不回来。

所以：
- 仓库名定了就别改
- 别换 GitHub 用户名
- 别从 GitHub Pages 换到 Netlify（反过来也一样）

**另外：装好之后，养成定期点「小本子 → 导出备份」的习惯。**
浏览器的数据说没就没（清缓存、换手机、iOS 回收存储），
导出那个 zip 存到「文件」App 里，才是真正丢不了的东西。

---

## ⚠️ 另一件必须知道的事

**电脑上的数据和手机上的数据是两套，不互通。**

浏览器的数据库是按"设备 + 浏览器"隔离的。你在电脑上 http://localhost:5173/ 加的待办，
手机上**看不到**。

这不是 bug，是浏览器安全模型。第 5 步我会做「导出 / 导入 JSON」来打通，
现在这一版：**选一个地方正经用，别两边都记。**

我的建议：**部署完就直接用手机那个**，电脑上只用来预览和改样式。

---

## 部署步骤（GitHub Pages）

### 准备

- 一个 GitHub 账号（你已经有了）
- 仓库必须是 **public（公开）** ← 私有仓库要开 Pages 得买 GitHub Pro

> 代码公开完全无所谓：**你的日记、待办、照片永远只存在你手机里，一个字节都不会上传。**
> GitHub 上放的只是一个空壳子。

### ① 新建仓库

1. 打开 https://github.com/new
2. **Repository name** 填：`todo-notebook`（或者你喜欢的名字，记住它，以后不能改）
3. 选 **Public**
4. **不要**勾 "Add a README file"
5. 点 **Create repository**

### ② 把文件传上去

打开 `C:\Users\a8252\Desktop\ios待办\`，**全选里面的内容**，然后：

1. 在刚建好的仓库页面点 **uploading an existing file**（或直接点 Add file → Upload files）
2. 把 `ios待办` 里**里面所有的东西**拖进上传框

   > 拖进去的应该是 `index.html`、`css`、`js`、`icons`、`server.cjs`、`start.bat`、
   > `manifest.webmanifest`、`sw.js` 这些，**不是** `ios待办` 这个文件夹本身。
   > 如果你看到仓库里第一层是 `ios待办/` 文件夹，那就错了，删掉重来。

   `tools` 文件夹可以不传，它只是我用来生成图标和跑测试的，传不传都行。

3. 底下 Commit changes 的说明随便写，点 **Commit changes**

### ③ 打开 GitHub Pages

1. 进仓库的 **Settings**（顶部齿轮那个）
2. 左边栏找到 **Pages**
3. **Source** 选 `Deploy from a branch`
4. **Branch** 选 `main`，右边目录选 `/ (root)`
5. 点 **Save**
6. **等 1–2 分钟**，刷新这个页面，顶部会出现一行绿底的网址：

   ```
   https://你的用户名.github.io/todo-notebook/
   ```

### ④ 装到 iPhone 上

1. iPhone 用 **Safari**（必须 Safari，Chrome 加不到主屏幕）
2. 打开上面那个网址
3. 点底部的 **分享按钮**（方框加箭头）
4. 往下滑，点 **「添加到主屏幕」**
5. 名字改成「本子」，点 **添加**

搞定。桌面上会出现一个薄荷绿的本子图标，点开就是全屏、没有地址栏。

---

## 以后改了代码怎么更新

1. 在仓库页面点 **Add file → Upload files**
2. 把改动过的文件（比如 `css/base.css`）拖进去覆盖
3. **Commit changes**
4. 等 1 分钟
5. 手机上打开本子，**下拉刷新一下**

我在 `sw.js` 里用的是「网络优先」策略，所以只要联网，刷新就能拿到新版，
不会出现"改了代码手机还是旧的"那种折磨人的情况。

> 万一真的没更新：Safari 设置 → 高级 → 网站数据 → 找到你的网址 → 删除。
> **注意这会连数据一起删掉**，所以第 5 步的导出备份很重要。

---

## 部署完之后，先验收这 5 件事

按顺序走一遍，都过了就说明装好了：

| | 验收项 | 怎么验 |
|---|---|---|
| 1 | **能打开、不是白屏** | 网址打开就是薄荷绿的本子，7 个时间块都在 |
| 2 | **加到主屏幕是全屏的** | 点开后**没有地址栏**，只有本子本身 |
| 3 | **数据存得住** | 加一条待办 → 完全退出 App → 再打开，还在 |
| 4 | **离线能用** | 开飞行模式 → 打开 App，还能正常用（有一次联网之后才会缓存） |
| 5 | **备份能导出** | 小本子 → 导出备份 → 弹出分享面板 → 存到「文件」App |

### 两个需要你实测的地方

**① PDF 导出**：`window.print()` 在**主屏幕 App 模式**下，iOS 各版本行为不太一样。
如果点了没反应，就打开**同一个网址用 Safari 直接访问**（不是从主屏幕图标进），
在那里点导出 PDF 就能出。

**② 导出备份**：走的是 iOS 系统分享面板。
如果主屏幕模式下弹不出来，同样退回 Safari 直接访问再试，
或者从「文件」App 里确认一下有没有存下来。

这两处要是都不行，告诉我，我换成别的实现方式。

### 关于 .nojekyll

文件夹里有个 `.nojekyll`（空的，看不见很正常）。
GitHub Pages 默认会拿 Jekyll 处理一遍站点，这个空文件让它跳过，部署更快也不会出怪问题。

**Windows 资源管理器默认不显示以点开头的文件**，所以拖拽上传时它可能没被带上——**不带也基本没事**
（这个项目里没有 Jekyll 会特殊处理的文件）。

想确保传上去的话，两个办法：
- 资源管理器「查看」→ 勾选「隐藏的项目」，再拖一次
- 或者在仓库页面点 **Add file → Create new file**，文件名填 `.nojekyll`，内容留空，直接提交

---

## 遇到问题

| 现象 | 原因 / 怎么办 |
|---|---|
| **白屏**，或者右上角红框报错 | 多半是文件层级传错了。仓库第一层必须直接就是 `index.html` |
| 页面能开但样式全乱 | `css` / `js` 文件夹没传全，或者大小写不对（GitHub Pages **区分大小写**） |
| 加到主屏幕后还是带地址栏 | 用的是 Chrome 或微信内置浏览器。**必须用 Safari** |
| 加不到主屏幕 | 网址必须是 https 开头。GitHub Pages 默认就是 https，检查 Pages 是否已生效 |
| 打开显示"数据库打不开" | 你是用 `file://` 双击打开的。改用 https 网址，或者 `start.bat` |
| Pages 里找不到入口 | 仓库是 Private。改成 Public，或者升级 GitHub Pro |

---

## 备选方案：Netlify Drop（更快，但要注册）

如果你嫌 GitHub 麻烦：

1. 注册 https://app.netlify.com （邮箱就行，不用信用卡）
2. 打开 https://app.netlify.com/drop
3. 把 `ios待办` 文件夹**整个**拖进去
4. 秒出一个 https 网址

**必须注册账号**：不登录拖出来的临时网址过一段时间会变，网址一变数据就没了。
登录后站点名字固定，以后重新拖还是同一个网址。

---

## 本地预览（改样式的时候用）

双击 `start.bat`，会自动打开 http://localhost:5173/

- 改完代码**直接刷新浏览器**就见效（服务器设了不缓存）
- 同一 WiFi 下，手机也能打开命令行里显示的那个 http://192.168.x.x:5173/ 看效果
  （但 http 不能加主屏幕，只能看，不能当 App 用）

---

## 自测

改完代码想确认没搞坏东西：

```
node tools\smoke.mjs
```

会自动开一个无头 Chrome，把渲染、加待办、刷新持久化、勾选进度、换色、九日小本子、
iPhone 尺寸下的字号、控制台报错全部跑一遍，最后打印多少通过多少失败。
